import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  colors: {
    primary: {},
    neutral: {},
  },
  fonts: {
    heading: `sans-serif`,
    body: `sans-serif`,
  },
})

export default theme
